export default defineAppConfig({
  ui: {
    button: {
      slots: {
        base: ["rounded-full cursor-pointer"],
      },
    },
    colors: {
      primary: "primary",
      neutral: "neutral",
    },
  },
});
